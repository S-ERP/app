import React, { Component } from 'react';
import { View, Text } from 'react-native';
import { SView } from 'servisofts-component';

export default class PopupExisteCarrito extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return <SView style={{
        width:"100%",
        height:"100%",
        position:"absolute",
        backgroundColor:"#f0f"
    }}>

    </SView>
  }
}
