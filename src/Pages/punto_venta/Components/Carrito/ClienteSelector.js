import React, { Component } from 'react';
import { View, Text } from 'react-native';

class ClienteSelector extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> ClienteSelector </Text>
      </View>
    );
  }
}

export default ClienteSelector;
